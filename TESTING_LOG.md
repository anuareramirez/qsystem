# QSystem — Manual Testing Log

Registro de pruebas manuales por navegador. Cada bug se resuelve en otra sesión de Claude Code; al volver, marcamos como `Resuelto` y validamos que no hubo regresión.

**Severidades:** `crítico` (bloquea uso) · `alto` (función rota) · `medio` (funciona pero mal) · `bajo` (cosmético/UX menor)

**Estados:** `Abierto` · `En fix` · `Resuelto (verificado)` · `No reproducible`

---

## Fase 1 — Autenticación y acceso

| # | Rol | Acción | Esperado | Observado | Severidad | Estado | Notas |
|---|-----|--------|----------|-----------|-----------|--------|-------|
| 1 | cualquiera | Login con contraseña incorrecta en `/auth/password-login` | Mensaje en español tipo "Usuario o contraseña incorrectos" | Mensaje crudo del backend en inglés: "No active account found with the given credentials" | bajo | Resuelto (verificado) | Override de `TokenObtainPairSerializer` en `apps/authentication/serializers.py` con `default_error_messages["no_active_account"] = "Usuario o contraseña incorrectos."`. Verificado vía curl: backend responde `{"detail":"Usuario o contraseña incorrectos."}` |
| 2 | cualquiera | Acceder a URL inexistente (ej. `/asdfqwerty`) | Página 404 con layout, mensaje en español y botón "Volver al inicio" | Página vacía solo con texto "404 - Page Not Found" sin estilos ni navegación | bajo | Resuelto (verificado) | Rediseño de `pages/errors/NotFound.jsx`: tarjeta con logo, dark mode (lee `localStorage.darkMode`), título "Página no encontrada", botones "← Regresar" (history) y "Volver al inicio" (`/` → `RootRedirect`) |
| 3 | admin | Bloqueo de usuarios por intentos fallidos — la UI dice "El bloqueo es por **combinacion de IP + usuario**" en `/management/configuracion/seguridad` | Bloquear solo la combinación IP+usuario que falló, otros usuarios desde la misma IP deberían poder seguir intentando | django-axes está configurado con `AXES_LOCKOUT_PARAMETERS=["ip_address","username"]` (OR, no AND), confirmado por el log "blocking by ip_address **or** username". Probado: intenté 5 logins fallidos como `user1` desde mi IP, después intenté login válido como `anuareramirez` (mismo IP) y aparece "Tu cuenta ha sido bloqueada temporalmente por seguridad. Intenta de nuevo en 30 minutos". El bloqueo bloqueó **toda la IP**, no la combinación. Riesgo: vector DoS — atacante puede bloquear admin solo intentando passwords erróneos repetidos | medio | Resuelto (verificado) | Cambio en `settings/base.py:280`: `AXES_LOCKOUT_PARAMETERS = [["ip_address", "username"]]` (lista anidada = AND lógico). Backend reloaded confirmando: log ahora dice "blocking by **combination of** ip_address and username" en lugar de "or". Solo se bloquea la combinación específica IP+usuario que falló; el resto de usuarios desde la misma IP siguen pudiendo intentar. Comentario inline en settings explica el "por qué" para evitar regresiones |

## Fase 2 — Catálogos y datos maestros

| # | Rol | Acción | Esperado | Observado | Severidad | Estado | Notas |
|---|-----|--------|----------|-----------|-----------|--------|-------|
| 4 | admin | `ClienteSerializer` validaciones — múltiples casos: email duplicado de cliente existente, email con formato inválido (`'invalid-email'`), celular con letras (`'abc123'`), `tipo_cliente=99` fuera de rango 1-4, name/last_name vacíos | El serializer rechaza con errores específicos por campo: `"email": "Ya existe..."`, `"email": "Formato inválido"`, `"celular": "Formato inválido"`, `"tipo_cliente": "Valor inválido"`, `"name": "Requerido"` | **Todos los casos pasan validación (`is_valid() == True`).** Probado vía Django shell con `ClienteSerializer(data=...).is_valid()`. La constraint del modelo (`unique=True` en email del User) probablemente captura el duplicado al hacer `save()`, pero la respuesta es un error genérico de DB en vez de un error 400 amigable por campo. Los demás casos (email inválido, celular con letras, tipo_cliente fuera de rango, campos vacíos) no se validan en absoluto | alto | Resuelto (verificado) | **Fix completo en `ClienteSerializer` (`apps/core/serializers.py`)**: agregados como write-only `email` (`EmailField`, valida formato y unicidad excluyendo el user actual en update), `name`/`last_name` (`CharField` con `allow_blank=False`), `tipo_cliente` (`ChoiceField(choices=Cliente.TIPO)` aliasado a `tipo` en `validate()`), y `validate_celular` que delega a `validate_phone()` (helper en `apps/core/validators.py` con regex `^\+?[\d\s\-().]+$` + ≥7 dígitos). `update()` propaga email/name/last_name al `User` asociado; `create()` los valida pero no los persiste (la creación oficial sigue pasando por `/users/create-client/`). También se aplicó `validate_phone` y `tipo` con choices en `ClienteDataSerializer` y `VendedorDataSerializer` (`apps/users/serializers.py`). **Verificado vía shell**: los 5 casos originales del bug ahora retornan `is_valid=False` con errores específicos por campo; un payload válido (`name`, `last_name`, `email`, `celular="6561234567"`, `tipo_cliente=2`) pasa correctamente |
| 1 | admin | Confirmar eliminación permanente desde papelera de Clientes | Mensaje con nombre del cliente, ej. "¿Está seguro de eliminar permanentemente a TestQA PruebaPreEntrega?" | Mensaje genérico sin nombre: "¿Está seguro de eliminar permanentemente el cliente? El cliente será eliminado permanentemente y no aparecerá en las listas." (suena raro al repetir "el cliente") | bajo | Resuelto (verificado) | `PermanentDeleteModal` ahora acepta prop `entityDisplayName`; `useTrashModeActions.handlePermanentDelete` la propaga; `ClientsTable` envía `${user.name} ${user.last_name}`. Mensaje nuevo: "¿Está seguro de eliminar permanentemente **TestQA PruebaPreEntrega**? Esta acción es irreversible y el registro no podrá recuperarse." Para el resto de tablas que aún no envían nombre, mensaje fallback simplificado sin repetición |
| 2 | admin | SearchableSelect (selector de Empresa en Crear Cliente) sin texto escrito | Mostrar hint o placeholder visible tipo "Escribe para buscar..." dentro del listbox abierto | Listbox completamente vacío al abrir el dropdown — usuario sin contexto sabe si la lista está vacía o si hay que escribir | bajo | Resuelto (verificado) | `SearchableSelect`: empty state con icono `FiInbox` y mensaje contextual: "No hay opciones disponibles" cuando `options=[]`, "Sin resultados" cuando la búsqueda no matchea. Nuevo prop `noOptionsText` para custom |
| 3 | admin | Acentos faltantes en labels, botones y placeholders de Catálogos | Acentos correctos en español: "Crear Clasificación", "Descripción", "Crear Área Temática", "Buscar áreas temáticas..." | Múltiples textos sin acentos: botón "Crear Clasificacion" (Catálogos→Clasificaciones), label "Descripcion" (Clasificaciones y Áreas Temáticas), botón "Crear Area Tematica", placeholder "Buscar areas tematicas..." | bajo | Resuelto (verificado) | Strings UI corregidos en `ClasificacionesSection.jsx` (toSingular), `AreasTematicasSection.jsx` (title + toSingular), `CatalogoForm.jsx` (labels API_MAP + label/placeholder Descripción), `CatalogosTable.jsx` (empty state + th), `CategoriasMaterialTable.jsx` y `TiposMaterialTable.jsx` (th Descripción). Ahora se ven: "Crear Clasificación", "Crear Área Temática", "Buscar áreas temáticas...", "Descripción". Nombres de variables JS y campo `descripcion` del API (snake_case backend) NO se tocaron |

## Fase 3 — Disponibilidad de instructores

| # | Rol | Acción | Esperado | Observado | Severidad | Estado | Notas |
|---|-----|--------|----------|-----------|-----------|--------|-------|
| 1 | admin | Abrir modal "Gestión de Disponibilidad" en dark mode (Editar Instructor → "Configurar Horarios de Disponibilidad") | Las cards internas del modal respetan dark mode (fondos oscuros, textos legibles) consistentes con el resto del modal | Las cards internas tienen fondo blanco sin variante dark, mientras el header y el wrapper del modal sí están en oscuro. Afecta: card "Gestión de Disponibilidad" (con selector instructor), card "Disponibilidades Actuales" (chips de días Lunes-Sábado con bordes blancos), tab "Bloques Específicos" → cards "Agregar Bloque Específico" y "Bloques Específicos Actuales". Los selects, inputs y botones también se ven sobre fondo blanco. Captura: `test-screenshots/dark-mode-disponibilidad.png`, `dark-mode-disp-2.png`, `dark-mode-bloques.png` | medio | Resuelto (verificado) | `AvailabilityForm.jsx` con dark mode completo siguiendo convención del proyecto: cards `bg-white dark:bg-gray-800` + `border-gray-200 dark:border-gray-700`, headings `text-gray-800 dark:text-gray-100`, labels `text-gray-700 dark:text-gray-300`, paneles `bg-gray-50 dark:bg-gray-700/50`, info azul `bg-blue-50 dark:bg-blue-900/30` + `text-blue-800 dark:text-blue-300`, inputs/selects con `dark:bg-gray-700 dark:border-gray-600 dark:text-gray-100`, bloques verdes/rojos con `bg-green-900/20`/`bg-red-900/20` + bordes 800. Vite HMR sin errores. Verificado: 0 instancias light-only en grep. |

## Fase 4 — Ventas (Cotizaciones)

| # | Rol | Acción | Esperado | Observado | Severidad | Estado | Notas |
|---|-----|--------|----------|-----------|-----------|--------|-------|
| 1 | admin | Click en una fila de cotización cerrada en `/management/ventas/cotizaciones_cerradas` | Abre modal de detalle con datos de cliente, cursos, PDF embebido y acciones disponibles según estado | Abre el modal "Editar Cotización Cerrada" en modo creación con error rojo: **"Error: No hay cliente o cursos de catálogo seleccionados"**. La URL no cambia a `/cotizaciones_cerradas/:id`. Probado con C17 (Aceptada) y la 3a fila — mismo error. Las cotizaciones abiertas (`/management/ventas/cotizaciones_abiertas` → click) sí abren correctamente como "Detalle Cotización Abierta" con PDF, datos y acciones. | alto | Resuelto (verificado) | `CotizacionesCerradasSection` ya no usa `ClosedQuotationForm` (era un form de creación que ignoraba `initialData`). Ahora usa el nuevo `ClosedQuotationDetailView` con `hideCreateButton`, simétrico al patrón de Abiertas. La vista internamente fetchea el detalle completo con `getCotizacionCerrada(id)` y renderiza el contenido compartido `QuotationDetailContent` (header, info, items, acciones, PDF) — mismo motor que el modal de /sales |
| 2 | admin | Abrir modal "Detalle Cotización Abierta" desde admin (cotización A7 de plaza ajena) — campo Cliente | Admin debería ver el nombre del cliente independientemente de la plaza dueña | Etiqueta "Cliente" aparece sola sin valor (vacía). Como seller en su propia cotización SÍ se muestra el cliente "Alejandra Valenzuela / ALC Global" correctamente. El bug es solo en vista de admin sobre cotizaciones de otras plazas | medio | Resuelto (verificado) | Causa raíz: el `CotizacionAbiertaListSerializer`/`CotizacionCerradaListSerializer` (usados por el endpoint LIST) no incluyen `cliente_detallado`/`cliente_empresa`, por eso la vista de detalle (que solo recibe la fila lista) los mostraba vacíos. Fix: `OpenQuotationDetailView` y `ClosedQuotationDetailView` ahora hacen fetch del detalle completo (`getCotizacionAbierta`/`getCotizacionCerrada`) al montar y muestran muestra "Cargando detalle…" mientras tanto. NO era problema de permisos: backend ya devuelve `is_plaza_owner=True` para admin |
| 3 | admin | Acciones disponibles en cotización Aceptada / Rechazada vista por admin | Admin como super-rol debería ver acciones aunque no sea dueño de la plaza | Vista de admin solo muestra "Ver PDF", "Descargar", "Enviar por Correo" en cotizaciones de otras plazas. Sin embargo, **seller en su propia cotización (A4) SÍ muestra acciones completas: Reenviar, Recotizar, Ficha Inscripción, Reasignar Curso, Rechazada (transición)**. Probablemente `is_plaza_owner` está restringiendo, lo cual es correcto para sellers en modo "Ver Todo", pero admin debería ver todo siempre | medio | Resuelto (verificado) | Causa raíz: el `OpenQuotationDetailView` viejo solo renderizaba 3 botones (Ver PDF/Descargar/Enviar Correo). NO usaba `QuotationActions` como /sales. Refactor: extraje el cuerpo de `QuotationViewModal` a `QuotationDetailContent` (mismo header, info, relaciones, `QuotationActions` + hooks `useQuotationStatus`/`useQuotationActions`, modales secundarios). Ambos `OpenQuotationDetailView` y `ClosedQuotationDetailView` lo usan. Admin ahora ve set completo (Reenviar, Recotizar, Ficha Inscripción, Reasignar Curso, transiciones) ya que `is_plaza_owner=True` para admin |
| 4 | admin | Click "Recotizar" en cotización abierta (A9 estado Enviada) y cerrada (C17 estado Aceptada) | Abrir QuotationModal con datos pre-llenos (cliente, curso, participantes, descuentos) y `isRequotation=true` para crear recotización; al guardar marca original como "Recotizada" y nueva como "Recotizada de A#/C#" | Click no abre ningún modal ni dialog. No hay confirmación, error ni efecto visible. Ni en cot. abierta (A9) ni en cot. cerrada (C17). Botón "Modificar" en C17 sí funciona correctamente. Otros botones (Reenviar, Ficha Inscripción, Reasignar Curso, Rechazada) sí funcionan | alto | Resuelto (verificado) | Doble causa raíz: (1) `<QuotationModal />` solo estaba montado en `Sales.jsx` y `CursoDetailPage.jsx`, no en `/management`, así que aunque `showQuotationModal=true` no había componente que renderizara la modal. (2) `QuotationContext` tenía un `useEffect` que detectaba `pathname` fuera de `/sales|/logistics|/accounting` y limpiaba `quotationMode` + `clearSelections()` inmediatamente — esto cancelaba la recotización en cuanto se disparaba desde `/management`. Fixes: agregar `/management` a `allowedPrefixes` en `QuotationContext.jsx`; montar `<QuotationModal />` (sin `ResponsiveQuotationSheet`) en `ManagementLayout.jsx`; guardar `if (onClose) onClose()` en `useQuotationActions.handleRecotizar` para que no rompa cuando el modal contenedor lo maneja SectionBase |
| 5 | admin | Cotización abierta en estado "Enviada" — buscar acción "Marcar como Aceptada" | Disponer de transición manual Enviada → Aceptada | **No es bug.** En cotización abierta el camino Enviada → Aceptada se hace **enviando la Ficha de Inscripción** (botón "Ficha Inscripción"). Validado: A9 (Hilda Martinez, ISO 31000) pasó de Enviada → Aceptada automáticamente al enviar la ficha. Mecánica de negocio correcta, no requiere botón "Marcar Aceptada" manual | bajo | No reproducible | Ficha Inscripción es la transición de aceptación implícita. La cotización pasa a Aceptada al confirmar inscripción del cliente |
| 6 | admin | Crear recotización de cotización Aceptada (A9) | La recotización debería iniciar en estado **Realizada** (recién creada, lista para enviar al cliente que aún no la ha aceptado). Solo la original cambia a Recotizada | A10 (recotización de A9) se crea directamente en estado **Aceptada**, herencia del estado de A9. Esto es incorrecto: el cliente todavía no ha aceptado la nueva versión. Cálculo correcto: 10 part × $3,400 = $34,000 base, -10% desc grupal = $30,600 ✅. Cadena "Recotizada de A9" mostrada correctamente en lista y modal | medio | No es bug | Aclaración del stakeholder: cuando la original ya está aceptada, la recotización **debe** quedar también en aceptada (es una modificación post-aceptación, no una nueva propuesta para el cliente). El comportamiento original era correcto. Reverti el cambio en `OpenQuotationForm.jsx` y `ClosedQuotationForm.jsx` — vuelve a propagar `aceptada` cuando `requotationData.originalEstado === "aceptada"` |
| 7 | admin | Activar servicios de comida (Desayuno/Comida/Coffee Break) en cotización cerrada | Si los servicios incrementan el costo (común en cotizaciones), el "Precio Total" debería actualizarse al activarlos. Si no incrementan, el toggle debería tener etiqueta clara explicando que es solo para registro/PDF | Toggle "Desayuno" se activa visualmente (bg-blue) pero el "Precio Total" se mantiene en $30,360 sin cambio. No hay aclaración de si afectan el monto. Posibles dos escenarios: (a) bug de cálculo si deberían sumar, (b) UX confuso si es informativo nada más | medio | Resuelto (verificado) | Decisión del stakeholder: **opción A** — los servicios suman al total. Backend ya implementaba Desayuno (`factor_8=$90/part`) y Comida (`factor_9=$200/part`); el problema era que Coffee Break se ignoraba. Cambios: (1) agregada variable `factor_10_cantidad` (default $50) en `initialize_sales_variables.py` y seedeada en BD via `python manage.py initialize_sales_variables`. (2) `pricing_cerrada.py` ahora lee `incluye_coffee` y suma `total_participantes × factor_10` cuando `lugar_id != "online"`; el desglose por item incluye `costo_coffee`. (3) UI ya cumple "no se puede activar en online": cuando `lugar_id === "online"` los toggles se ocultan y se muestra "No disponible para cursos en línea"; al cambiar lugar a online se resetean `incluye_desayuno/comida/coffee` a false (líneas 742-745). Si user no vio el cambio antes, era porque (a) la lógica de Coffee no estaba implementada y/o (b) no esperaba los ~500ms de debounce + roundtrip al backend |
| 8 | admin | Cambiar "Grupos" de 1 a 2 en cotización cerrada (5S) | Sumar otro grupo de 10 participantes con el mismo precio por curso, aplicar descuento por 2 grupos (15%) | El descuento del 15% aplica correctamente ($62,520 → $53,142), pero al cambiar a 2 grupos: (a) "Precio Unitario" cambia de $3,795 a $1,942.50 sin explicación visible, (b) "Precio por Grupo" muestra "(20 part.)" cuando "Part./Grupo" estaba en 10 (no se duplica el campo, pero el label lo asume). El cliente ve un precio unitario cambiante que confunde el desglose | medio | Resuelto (verificado) | Doble bug en `ClosedQuotationForm.jsx` línea ~866: (1) "Precio Unitario" dividía por `(totalParticipantes × num_grupos)` — `totalParticipantes` ya es `num_grupos × num_participantes`, así que el denominador era `num_grupos² × num_participantes` → con 2 grupos el unitario se reducía artificialmente a la mitad. Cambiado a `Math.max(totalParticipantes, 1)` → ahora el unitario por participante se mantiene estable al variar grupos. (2) Label "Precio por Grupo (X part.)" usaba `totalParticipantes` (acumulado de todos los grupos). Cambiado a `getParticipantesValue(item)` (per-grupo), consistente con que el monto mostrado ya está dividido por `num_grupos`. Sin cambios de backend |

## Fase 5 — Logística

| # | Rol | Acción | Esperado | Observado | Severidad | Estado | Notas |
|---|-----|--------|----------|-----------|-----------|--------|-------|
| 3 | admin | Editar ficha de inscripción y cambiar `estado` desde el modal — probado con ficha 11 (cot abierta A13) | El PUT `/api/logistica/fichas/{id}/` acepta el cambio de estado y persiste; UI refresca con nuevo estado | El select del modal sí cambia el value (`pendiente` → `en_proceso`), pero el submit "Actualizar Ficha" dispara `PUT /api/logistica/fichas/11/` con respuesta **400 Bad Request** (3 intentos consecutivos en logs). El estado en BD permanece `pendiente`. Validado vía Django shell: el modelo SÍ acepta cualquier estado (`fs.estado='en_proceso'; fs.save()` funciona y dispara side effects correctos: cancelar ficha degrada curso a PROSPECTADO y soft-deletea comisión auto-generada). El bug está en el serializer del PUT — algún campo del payload no pasa validación | alto | Abierto | Inspeccionar `FichaDeInscripcionSerializer` (o el ViewSet PUT handler) para ver qué validación está rechazando. Posibles causas: (1) algún campo requerido no se manda; (2) el cambio de estado tiene reglas que no permiten pendiente→en_proceso desde edit; (3) campos `*_temp` (nombre_empresa_temp, nombre_contacto_temp, etc.) tienen validación que el form no llena. **Las transiciones internas funcionan** — solo falla el endpoint de UPDATE |
| 1 | admin | Sección Participantes (`/management/logistica/participantes`) — seleccionar ficha A1 que dice (5/5) en el selector | Tabla muestra los 5 participantes de la ficha A1 con sus columnas (Nombre, CURP, Puesto, Email, Teléfono, Confirmado) para verlos y editarlos | Header de ficha confirma "Participantes 5 / 5" pero la tabla muestra "👥 No hay participantes / Selecciona una ficha para ver sus participantes". Los participantes existen en BD (el contador 5/5 viene del backend) pero no se cargan en la tabla. El propósito de esta sección es VER y EDITAR participantes existentes (no agregarlos), por lo que sin tabla la sección no cumple su función | alto | Resuelto (verificado) | Causa raíz: mismatch de contrato entre frontend y backend. El endpoint `GET /logistica/fichas/{id}/participantes/` devolvía `{ ficha_id, total_participantes, participantes: [...] }`, pero `ParticipantesSection.jsx` lee `pageData.results` y `pageData.count` (formato paginado estándar de DRF). Como ambos campos eran undefined → tabla vacía. Fix en `views/fichas_inscripcion.py::listar_participantes`: ahora usa `paginate_queryset()` + `get_paginated_response()` para devolver `{ count, next, previous, results }`. Además acepta `?search=` y filtra por nombre, apellido (paterno/materno), CURP, email, puesto y teléfono. `VendorParticipantRegistrationModal` no se rompe porque ya tenía fallback a `response.data.results`. Verificado: ficha 1 tiene 5 participantes en BD, ahora aparecen en la tabla |
| 2 | admin | Buscar en input "Buscar participantes..." con un fragmento de CURP (ej. "HCHMLN11") teniendo seleccionada la ficha A1 | Filtrar tabla server-side, mostrar solo el participante con CURP que matchea | Endpoint `GET /api/logistica/fichas/1/participantes/?search=HCHMLN11` responde **404 Not Found**. Tabla queda vacía. Sin parámetro search responde 200 OK con los 5 participantes | medio | Resuelto (verificado) | **Diagnóstico**: el `SearchFilter` del ViewSet padre (con `search_fields = [nombre_empresa_temp, nombre_contacto_temp, apellido_contacto_temp]`) interceptaba `?search=` ANTES del lookup por pk, dejaba la queryset de fichas vacía y `self.get_object()` devolvía 404. **Primer intento** usó `from django.shortcuts import get_object_or_404` pero ruff stripeó el import como aparentemente no usado, dejando el endpoint con `NameError: name 'get_object_or_404' is not defined` → 500 (regresión que también rompía el bug Fase 5 #1). **Fix definitivo en `views/fichas_inscripcion.py::listar_participantes`**: lookup inline con el modelo (que ya estaba importado) — `try: ficha = self.get_queryset().get(pk=pk) except FichaDeInscripcion.DoesNotExist: return Response({...}, status=404)` + `self.check_object_permissions(request, ficha)`. No depende de imports que ruff pueda stripear. Backend reloaded sin errores; smoke check del endpoint sin auth devuelve 401 (ruta válida) |
| 3 | admin | Acentos faltantes en formularios de Receso y Material Entregado | Acentos correctos en español: "Servicios de Alimentación", "¿Está seguro...?", "Esta acción", "ID Material Curso Catálogo" | `RecesoForm.jsx`: subtítulo "Servicios de **Alimentacion**", confirm "**Esta seguro** de eliminar este receso? Esta **accion** no se puede deshacer." `MaterialEntregadoForm.jsx`: label "ID Material Curso **Catalogo** *", confirm "**Esta seguro** de eliminar este registro?" — todos sin acentos. Mismo patrón de Fase 2 #3 e Importaciones | bajo | Resuelto (verificado) | Strings UI corregidos: `RecesoForm.jsx` línea 199 → "Servicios de Alimentación", línea 348 → "¿Está seguro de eliminar este receso? Esta acción no se puede deshacer.". `MaterialEntregadoForm.jsx` línea 137 → "ID Material Curso Catálogo *", línea 297 → "¿Está seguro de eliminar este registro?". Nombres de variables JS y campos snake_case del backend NO se tocaron |

## Fase 6 — Contabilidad

| # | Rol | Acción | Esperado | Observado | Severidad | Estado | Notas |
|---|-----|--------|----------|-----------|-----------|--------|-------|
| 1 | admin | Detail page de un costo (`/accounting/costos/:id`) — buscar acción para eliminar | Botón "Eliminar Costo" que mueva a papelera y luego permita eliminar permanente | **NO existe botón Eliminar.** Header muestra solo: ← Autorizado / Registrar Comprobante / Reasignar. Tabs: General / Comprobantes / Archivos. No hay forma desde la UI de eliminar un costo creado por error (probado con "TestQA-Gasto" creado vía /accounting/egresos/nuevo, costo id 83). Requirió delete vía Django shell | medio | Resuelto (verificado) | `CostoDetailContent`: nuevo botón rojo con `FiTrash2` en el header (después de Reasignar) + `ConfirmDialog` con mensaje "¿Está seguro de eliminar este costo? Pasará a la papelera y podrá restaurarlo desde ahí o eliminarlo permanentemente." Handler `handleDelete` llama a `deleteCosto(costoId)` (soft-delete vía `/logistica/costos/{id}/soft-delete/`, ya existente en `api/logistics.js`) y dispara prop `onDeleted` para que el contenedor navegue/cierre. Wired en los 3 consumers: `CostoDetailPage` → `handleBack`, `ComprobantesGastoSection` y `EgresosSection` → cierra el modal y recarga la lista. Permanent-delete sigue viviendo en `useTrashModeActions` (papelera), igual que el resto del sistema |
| 2 | admin | Factura con todos sus pagos en estado "Rechazado" — verificar estado de la factura | Si todos los pagos están rechazados, la factura debería volver a "Generada" porque el saldo regresa al total y no hay pagos pendientes/confirmados | **Factura permanece en "En Proceso de Pago"** aunque su único Pago esté en estado `rechazado`. Probado: factura 3, total $15,968.57, registré pago parcial $100 (pasa a Confirmado, factura → "En Proceso de Pago", saldo $15,868.57), luego "Revertir a Pendiente" → "Rechazar" con motivo. Saldo se restaura a $15,968.57 ✅ pero `factura.estado='en_proceso_pago'` queda pegado. Usuario tiene que dar click manual a "← Generada" para corregir | medio | Resuelto (verificado) | Nuevo método `Factura.recalcular_estado()` en `apps/contabilidad/models.py` que mira los pagos activos (`confirmado`/`pendiente`, no eliminados): saldo==0 → `pagada`, hay pagos activos → `en_proceso_pago`, sin pagos activos → `generada` (limpia `fecha_pago` si deja de estar pagada). Respeta estados terminales `borrador`/`cancelada`. Llamado desde las 3 actions de `PagoViewSet`: `confirmar`, `revertir` y `rechazar`. Ahora rechazar el único pago de una factura la regresa a `generada` automáticamente |
| 3 | admin | Estado del Pago — al registrar un pago nuevo desde la factura | Pago se crea en estado "Pendiente de Confirmar" (default seguro: necesita aprobación contable antes de confirmarlo) | Pago se crea **directamente en estado "Confirmado"** sin paso intermedio. La transición Pendiente → Confirmado existe (botón "Confirmar" cuando está en pendiente; "Revertir a Pendiente" cuando está en confirmado), pero el flujo de creación se la salta. No hay opción en el modal "Registrar Pago" para crearlo como pendiente | bajo | Resuelto (verificado) | `PagoViewSet.create` ahora hace `serializer.save(estado="pendiente", fecha_confirmacion=None)` en vez de `"confirmado"`. La factura aún pasa a `en_proceso_pago` cuando aparece el primer pago (aunque esté pendiente), pero NO llega a `pagada` hasta que el pago se confirme — eso lo maneja la action `confirmar` que ya existía. La transición pendiente↔confirmado funcional desde la UI con los botones de detalle |
| 4 | admin | Costo en estado "Pagado" con su comprobante en "Aprobado" — Revertir comprobante a Pendiente y volver a Aprobar | Al re-aprobar el comprobante, el costo asociado debe **mantener su estado "Pagado"** (un comprobante recién aprobado no implica que el pago se haya deshecho) | Al hacer click en "Aprobar" desde Pendiente, el costo **degrada automáticamente de "Pagado" → "Comprobado"**. Probado con Costo 50 (Comida, Lic. Araceli Mota): estaba en "Pagado", revertí comprobante 7 (Aprobado→Pendiente), el costo siguió en "Pagado" ✅. Volví a Aprobar el comprobante y el costo bajó a "Comprobado" ❌. El usuario pierde manualmente el estado Pagado y debe volver a hacer click en "Pagado" para subirlo. Restaurado vía shell | medio | Resuelto (verificado) | En `ComprobanteGastoViewSet.aprobar` (`apps/contabilidad/views.py:479`): cambiada la condición de auto-transición de `costo.estado in ["autorizado", "pagado"]` a `costo.estado == "autorizado"`. Ahora aprobar un comprobante solo bumpea costos en "autorizado" → "comprobado". Si el costo ya está en "pagado", no se toca (la elección manual del usuario se respeta). Comentario inline explica el por qué para evitar regresiones |
| 5 | admin | Acentos en label "Comprobante generado automaticamente" en lista `/accounting/egresos?view=comprobantes` | "automáticamente" con tilde | Aparece en filas con proveedor "N/A" como **"Comprobante generado automaticamente"** sin tilde. Mismo patrón de Fase 2 #3, Fase 5 #4, Fase 7 #1 | bajo | Resuelto (verificado) | El string vivía en `concepto` de filas creadas por la migración 0013. Nueva data migration `contabilidad/0016_fix_concepto_acento.py` que hace `UPDATE comprobantes SET concepto='Comprobante generado automáticamente' WHERE concepto='Comprobante generado automaticamente'`. Verificado: 4 filas corregidas en BD de dev (sin acento: 0, con acento: 4). Reverse sí restaura el string viejo por seguridad |
| 6 | admin | Avanzar Costo de Comprobado → Pagado con click en botón "Pagado" | Toast con mensaje afirmativo tipo "Costo marcado como pagado" (mismo que Autorizado→Pagado) | Toast muestra **"Estado revertido exitosamente"**, mensaje genérico de revert que no aplica a un forward. Probado con Costo 81 (Honorarios Instructor): Autorizado→Pagado mostró "Costo marcado como pagado" ✅, pero después de Comprobado→Pagado mostró "Estado revertido exitosamente" ❌. UX confunde porque sugiere que la acción retrocedió cuando en realidad avanzó | bajo | Resuelto (verificado) | `CostoDetailContent.handleRevertir` ahora muestra el mensaje contextual con el destino: `"Costo marcado como ${ESTADOS_FLOW[currentIndex - 1]?.label}"` (e.g., "Costo marcado como Pagado", "Costo marcado como Autorizado"). Esto evita el "Estado revertido" genérico que confundía al usuario porque desde su POV ir Comprobado → Pagado es un avance |
| 7 | admin | Placeholders/labels sin acentos en modal "Registrar Comprobante" (`/accounting/costos/:id` → "Registrar Comprobante") | "Descripción del gasto", "Comisión del Vendedor" con acentos | Placeholder del campo Concepto: **"Descripcion del gasto"** sin tilde. Opción del select Tipo de Gasto: **"Comision del Vendedor"** sin tilde. Mismo patrón de Fase 2 #3, Fase 5 #4, Fase 7 #1, Fase 6 #5 | bajo | Resuelto (verificado) | Strings UI corregidos: `CostoDetailContent.jsx` línea 301 (option label `"Comisión del Vendedor"`) y línea 1291 (placeholder `"Descripción del gasto"`). `ComprobanteGastoForm.jsx` línea 21 (mismo option label `"Comisión del Vendedor"`). El value del choice (`comision_vendedor`, snake_case del backend) NO se toca |

## Fase 7 — Mailings e Imports

| # | Rol | Acción | Esperado | Observado | Severidad | Estado | Notas |
|---|-----|--------|----------|-----------|-----------|--------|-------|
| 1 | admin | Acentos faltantes en sección Importaciones (`/management/configuracion/importaciones`) | "Crear Importación", "Tipo de Importación", "Cursos de Catálogo" con acentos correctos | Modal y selector usan: botón "Crear **Importacion**", header "Crear **Importacion**", label "Tipo de **Importacion**", opción "Cursos de **Catalogo**" — todos sin acentos. Mismo patrón del bug ya resuelto en Fase 2 #3 (Catálogos) pero aquí persiste | bajo | Resuelto (verificado) | Strings UI corregidos en `ImportacionesSection.jsx` (toSingular `"Importación"`) y `ImportJobForm.jsx`: opción `"Cursos de Catálogo"`, label `"Tipo de Importación *"`, botones `"Crear Importación"` / `"Procesar Importación"`, título `"Eliminar Importación"`, mensaje `"¿Está seguro de eliminar esta importación? Esta acción no se puede deshacer."`, todos los toasts (`"Importación creada exitosamente"`, `"Importación finalizada"`, `"La importación falló"`, `"Importación eliminada"`, `"Error al crear/procesar/eliminar importación"`). Plural `"Importaciones"`/`"importaciones"` se mantiene sin tilde (aguda → grave terminada en s). Nombre de componente JS y campo snake_case `cursos_catalogo` no se tocaron |

## Fase 8 — Vista del cliente

| # | Rol | Acción | Esperado | Observado | Severidad | Estado | Notas |
|---|-----|--------|----------|-----------|-----------|--------|-------|
|   |     |        |          |           |           |        |       |

## Fase 9 — Cross-cutting

| # | Rol | Acción | Esperado | Observado | Severidad | Estado | Notas |
|---|-----|--------|----------|-----------|-----------|--------|-------|
|   |     |        |          |           |           |        |       |
